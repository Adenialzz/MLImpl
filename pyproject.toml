[project]
name = "mlimpl"
version = "0.1.0"
description = "Machine Learning Simple Implementations"
readme = "README.md"
requires-python = "==3.11.14"
dependencies = [
    "torch",
    "numpy",
    "ipykernel",
    "ipywidgets",
    "tqdm",
    "matplotlib",

]
